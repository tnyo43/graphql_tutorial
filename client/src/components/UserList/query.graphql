fragment UserListItem on User {
  githubLogin
  name
  avatar
}

query allUsers {
  totalUsers
  allUsers {
    ...UserListItem
  }
}

mutation addFakeUsers($count: Int!) {
  addFakeUsers(count: $count) {
    ...UserListItem
  }
}
